package prog

// Each Card object represents a single playing card
// The class contains nothing except two attributes: The suit of the card, and the symbol on the card
data class Card constructor(val symbol : String?, val suit : String?) { }
